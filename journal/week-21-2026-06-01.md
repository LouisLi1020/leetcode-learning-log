# 第 21 週 · Day 141–146

**日期：** 2026/06/01 – 2026/06/06  
**語言：** Java（主）· C# · Python（輔）

[← 目錄](../README.md) · [← 上一週](week-20-2026-05-25.md)

---

## 2026/06/01 · Day 141

### 今日任務

- [5. Longest Palindromic Substring](https://leetcode.com/problems/longest-palindromic-substring/)

---

### LC 5 · Longest Palindromic Substring

| 項目 | 內容 |
|------|------|
| Pattern | DP / expand |
| 難度 | Medium |

**題意：** 求最長回文子字串。

**核心：** **中心擴展：** 枚舉中心（單/雙字元），向兩側擴；或 DP `P[i][j]`。

```java
public String longestPalindrome(String s) {
    int start = 0, maxLen = 0;
    for (int i = 0; i < s.length(); i++) {
        int len1 = expand(s, i, i);
        int len2 = expand(s, i, i + 1);
        int len = Math.max(len1, len2);
        if (len > maxLen) {
            maxLen = len;
            start = i - (len - 1) / 2;
        }
    }
    return s.substring(start, start + maxLen);
}
int expand(String s, int l, int r) {
    while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) {
        l--; r++;
    }
    return r - l - 1;
}
```

**複雜度：** 時間 O(n²) · 空間 O(1)


---

## 2026/06/02 · Day 142

### 今日任務

- [97. Interleaving String](https://leetcode.com/problems/interleaving-string/)

---

### LC 97 · Interleaving String

| 項目 | 內容 |
|------|------|
| Pattern | DP 2D |
| 難度 | Medium |

**題意：** 判斷 `s3` 是否為 `s1`、`s2` 交錯組成。

**核心：** `dp[i][j]`：前 `i+j` 字元能否由 `s1[0..i)` 與 `s2[0..j)` 交錯；轉移看 `s1[i-1]` 或 `s2[j-1]` 是否匹配 `s3[i+j-1]`。

```java
public boolean isInterleave(String s1, String s2, String s3) {
    int m = s1.length(), n = s2.length();
    if (m + n != s3.length()) return false;
    boolean[][] dp = new boolean[m + 1][n + 1];
    dp[0][0] = true;
    for (int i = 1; i <= m; i++) dp[i][0] = dp[i - 1][0] && s1.charAt(i - 1) == s3.charAt(i - 1);
    for (int j = 1; j <= n; j++) dp[0][j] = dp[0][j - 1] && s2.charAt(j - 1) == s3.charAt(j - 1);
    for (int i = 1; i <= m; i++)
        for (int j = 1; j <= n; j++)
            dp[i][j] = (dp[i - 1][j] && s1.charAt(i - 1) == s3.charAt(i + j - 1))
                    || (dp[i][j - 1] && s2.charAt(j - 1) == s3.charAt(i + j - 1));
    return dp[m][n];
}
```

**複雜度：** 時間 O(mn) · 空間 O(mn)


---

## 2026/06/03 · Day 143

### 今日任務

- [72. Edit Distance](https://leetcode.com/problems/edit-distance/)

---

### LC 72 · Edit Distance

| 項目 | 內容 |
|------|------|
| Pattern | DP 2D |
| 難度 | Medium |

**題意：** 將 `word1` 變成 `word2` 的最少操作（增刪替）。

**核心：** `dp[i][j]`：若 `w1[i-1]==w2[j-1]` 則 `dp[i-1][j-1]`；否則 `1+min(上,左,左上)`。

```java
public int minDistance(String word1, String word2) {
    int m = word1.length(), n = word2.length();
    int[][] dp = new int[m + 1][n + 1];
    for (int i = 0; i <= m; i++) dp[i][0] = i;
    for (int j = 0; j <= n; j++) dp[0][j] = j;
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (word1.charAt(i - 1) == word2.charAt(j - 1))
                dp[i][j] = dp[i - 1][j - 1];
            else
                dp[i][j] = 1 + Math.min(dp[i - 1][j - 1],
                        Math.min(dp[i - 1][j], dp[i][j - 1]));
        }
    }
    return dp[m][n];
}
```

**複雜度：** 時間 O(mn) · 空間 O(mn)


---

## 2026/06/04 · Day 144

### 今日任務

- [123. Best Time to Buy and Sell Stock III](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iii/)

---

### LC 123 · Best Time to Buy and Sell Stock III

| 項目 | 內容 |
|------|------|
| Pattern | DP |
| 難度 | Hard |

**題意：** 最多 **2 次** 交易的股票最大利潤。

**核心：** **狀態 DP：** `buy1,sell1,buy2,sell2` 滾動；或 `dp[k][hold]`。與 LC121/122 同系列。

| 狀態 | 轉移 |
|------|------|
| buy1 | max(buy1, -price) |
| sell1 | max(sell1, buy1+price) |
| buy2 | max(buy2, sell1-price) |
| sell2 | max(sell2, buy2+price) |

part1 陣列題亦含 123；此處以 **2D/狀態 DP** 視角完整記錄。

```java
public int maxProfit(int[] prices) {
    int buy1 = Integer.MAX_VALUE, sell1 = 0;
    int buy2 = Integer.MAX_VALUE, sell2 = 0;
    for (int p : prices) {
        buy1 = Math.min(buy1, p);
        sell1 = Math.max(sell1, p - buy1);
        buy2 = Math.min(buy2, p - sell1);
        sell2 = Math.max(sell2, p - buy2);
    }
    return sell2;
}
```

**複雜度：** 時間 O(n) · 空間 O(1)


---

## 2026/06/05 · Day 145

### 今日任務

- [188. Best Time to Buy and Sell Stock IV](https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iv/)

---

### LC 188 · Best Time to Buy and Sell Stock IV

| 項目 | 內容 |
|------|------|
| Pattern | DP |
| 難度 | Hard |

**題意：** 最多 **k 次** 交易的最大利潤。

**核心：** `dp[t][0/1]` 第 t 次交易後不持倉/持倉最大利；`k` 大時等價無限次（LC122）。

```java
public int maxProfit(int k, int[] prices) {
    int n = prices.length;
    if (k >= n / 2) {
        int profit = 0;
        for (int i = 1; i < n; i++)
            if (prices[i] > prices[i - 1]) profit += prices[i] - prices[i - 1];
        return profit;
    }
    int[] buy = new int[k + 1], sell = new int[k + 1];
    Arrays.fill(buy, Integer.MIN_VALUE / 2);
    for (int p : prices) {
        for (int t = 1; t <= k; t++) {
            buy[t] = Math.max(buy[t], sell[t - 1] - p);
            sell[t] = Math.max(sell[t], buy[t] + p);
        }
    }
    return sell[k];
}
```

**複雜度：** 時間 O(nk) · 空間 O(k)


---

## 2026/06/06 · Day 146

### 今日任務

- [221. Maximal Square](https://leetcode.com/problems/maximal-square/)

---

### LC 221 · Maximal Square

| 項目 | 內容 |
|------|------|
| Pattern | DP grid |
| 難度 | Medium |

**題意：** 全為 1 的最大正方形邊長。

**核心：** `dp[i][j]`：若 `matrix[i][j]=='1'` 則 `1+min(上,左,左上)`；更新最大邊長。

```java
public int maximalSquare(char[][] matrix) {
    int m = matrix.length, n = matrix[0].length;
    int[][] dp = new int[m + 1][n + 1];
    int side = 0;
    for (int i = 1; i <= m; i++) {
        for (int j = 1; j <= n; j++) {
            if (matrix[i - 1][j - 1] == '1') {
                dp[i][j] = 1 + Math.min(dp[i - 1][j],
                        Math.min(dp[i][j - 1], dp[i - 1][j - 1]));
                side = Math.max(side, dp[i][j]);
            }
        }
    }
    return side * side;
}
```

**複雜度：** 時間 O(mn) · 空間 O(mn)


---

## 本週小結

本週專注 **Multidimensional DP**，共 6 題；建議週末用 15 分鐘口述每題核心與複雜度。

| 主題 | 題號 |
|------|------|
| Multidimensional DP | 5, 97, 72, 123, 188, 221 |
